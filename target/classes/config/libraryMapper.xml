<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="lib">



<select id="count_number" resultType="int">
	select count(MEM_ID) from MEMBER
</select>

<select id="count_like" resultType="int">
	select count(SHOP_NUM) from SHOPPING_LIKE
</select>

<select id="count_product" resultType="int">
	select count(*) from shopping
</select>

<!-- 
<select id="count_product" resultType="int">
	select count(SALES_CNT) from shopping_sales
</select> 
-->

</mapper>
